version: 2

models:
  - name: dim_products
    description: Dimensão de funcionários da Northwind.
    columns:       
      - name: product_id
        description: primary key for product table
        tests:
          - unique
          - not_null
